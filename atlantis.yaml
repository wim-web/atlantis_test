version: 3
projects:
  - name: sample
    dir: terraform/sample
    workflow: custom
workflows:
  custom:
    plan:
      steps:
        - run: touch dummy.tfplan
        - run: echo hello
    apply:
      steps:
        - run: echo hello-2
